# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

Project is pinned to SDK 54 to match the current Expo Go app store release — do not bump to a newer SDK without checking Expo Go's supported version first.
